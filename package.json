{
  "name": "sharadha-stores-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace configuration for Sharadha Stores. Allows running scripts from the root directory.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install-all": "npm install --prefix frontend"
  }
}
